- if flash[:error]
  %p{ :class => 'message error' }= flash[:error]

%form{ :action => url_for('/sets'), :method => 'GET' }
  %input{ :value => @person.username || 'Enter Flickr ID', :name => 'u', :type => 'text' }
